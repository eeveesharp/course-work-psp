@model List<matallurgical_plant.Models.Product>

@{
    ViewData["Title"] = "Home Page";
}

@*<div class="text-center">
    <h1 class="display-4">FUCKINGCOURSE</h1>
    

    @Html.ActionLink("Add product ", "Add")
    <table>
        <tr>
            <th>Название</th>
            <th>Цена</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(model => item.NameProduct)
                </td>

                <td>
                    @Html.DisplayFor(model => item.Price)
                </td>

                <td>
                    @Html.ActionLink("Edit ", "Edit", new { id = item.Id ,item})
                    @Html.ActionLink("Details ", "Details", new { id = item.Id })
                    @Html.ActionLink("Delete ", "Delete", new { id = item.Id })
                </td>

            </tr>
        }
    </table>*@

    
</div>
<div class="text-center">
    <h2 class="display-4">Welcome</h2>
    <h3>@Html.ActionLink("Login", "Login", "Account")</h3>
    <h3>@Html.ActionLink("Register", "Register", "Account")</h3>
</div>
